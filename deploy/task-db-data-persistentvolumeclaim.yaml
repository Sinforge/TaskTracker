apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: task-db-data
  name: task-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
